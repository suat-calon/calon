import { AuroraSurface } from "@/components/aurora/aurora-surface";

export const metadata = {
  title: "CALON — Aurora UI Preview",
  description: "Premium UI direction preview for Calon Business OS",
};

export default function AuroraPage() {
  return <AuroraSurface />;
}
