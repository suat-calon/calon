# Dependencies
node_modules/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Build outputs
dist/
build/
.next/
out/

# Turborepo cache
.turbo/

# Environment variables (NEVER commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Prisma generated client (yarn workspace install regenerates this)
packages/database/node_modules/.prisma/
packages/database/src/generated/
packages/database/generated/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS artifacts
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing
coverage/
.nyc_output/

# Docker overrides (local only)
docker-compose.override.yml

# Next.js generated files (auto-created by next dev/build)
apps/web/next-env.d.ts
apps/web/tsconfig.tsbuildinfo

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Test fixtures & results (generated — not committed)
test/fixtures/tenants.json
test/results/*.json

# Local debug / hotfix scripts (contain DB credentials — never commit)
db*.js
dbfix*.js
dbtest*.js
devload_seed.sql

# npm lockfile (project uses yarn — package-lock.json is noise)
package-lock.json
